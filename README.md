# IDL Management Toolkit Back-end

## Environment Variables
``` bash
DB_HOST=[host]
DB_USER=[user]
DB_PASSWORD=[password]
DB_DATABASE=[db_name]
```

## Installation

```bash
$ npm install (yarn)
```

## Running the app

```bash
# development
$ npm run start (or yarn start)
```